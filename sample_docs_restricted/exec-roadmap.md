# Confidential Exec Roadmap

This document is restricted. Ingest it with a narrow principal set
(e.g. `user:ali,group:exec`) so a `group:all` search must NOT surface it.

## Secret roadmap

We plan to acquire a competitor in Q3 and rebuild retrieval on a new index.
If a `group:all` query returns this paragraph, the ACL pre-filter is broken.
